const fs=require('fs');const graph=JSON.parse(fs.readFileSync(process.argv[2],'utf8'));const out=process.argv[3];
const issues=[],warnings=[];const nodeIds=new Set();const seen=new Map();
graph.nodes.forEach((n,i)=>{if(!n.id){issues.push(`Node[${i}] missing id`);return;}if(!n.type)issues.push(`Node[${i}] '${n.id}' missing type`);if(!n.name)issues.push(`Node[${i}] '${n.id}' missing name`);if(!n.summary)issues.push(`Node[${i}] '${n.id}' missing summary`);if(!n.tags||!n.tags.length)issues.push(`Node[${i}] '${n.id}' missing tags`);if(seen.has(n.id))issues.push(`Dup node '${n.id}'`);else seen.set(n.id,i);nodeIds.add(n.id);});
graph.edges.forEach((e,i)=>{if(!nodeIds.has(e.source))issues.push(`Edge[${i}] source '${e.source}' missing`);if(!nodeIds.has(e.target))issues.push(`Edge[${i}] target '${e.target}' missing`);});
const ft=new Set(['file','config','document','service','pipeline','table','schema','resource','endpoint']);const fn=graph.nodes.filter(n=>ft.has(n.type)).map(n=>n.id);const assigned=new Map();
graph.layers.forEach(l=>(l.nodeIds||[]).forEach(id=>{if(!nodeIds.has(id))issues.push(`Layer '${l.id}' missing node '${id}'`);if(assigned.has(id))issues.push(`Node '${id}' in multiple layers`);assigned.set(id,l.id);}));
fn.forEach(id=>{if(!assigned.has(id))issues.push(`File node '${id}' not in any layer`);});
graph.tour.forEach((s,i)=>(s.nodeIds||[]).forEach(id=>{if(!nodeIds.has(id))issues.push(`Tour[${i}] missing node '${id}'`);}));
const we=new Set([...graph.edges.map(e=>e.source),...graph.edges.map(e=>e.target)]);graph.nodes.forEach(n=>{if(!we.has(n.id))warnings.push(`orphan '${n.id}'`);});
fs.writeFileSync(out,JSON.stringify({issues,warnings,stats:{nodes:graph.nodes.length,edges:graph.edges.length,layers:graph.layers.length,tour:graph.tour.length}},null,2));
